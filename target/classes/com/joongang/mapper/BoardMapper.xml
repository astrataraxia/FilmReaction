<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.joongang.mapper.BoardMapper">

<select id="getList" resultType="com.joongang.domain.BoardVO">
	select * from film_board
</select>

<select id="getListWithPaging" resultType="com.joongang.domain.BoardVO">
	select @rownum := @rownum + 1 as  bno, title, content, writer, regdate, updatedate
		from film.film_board, (select @rownum := 0) A
		order by bno desc  LIMIT #{offset}, #{amount}
</select>

<select id="boardid" resultType="com.joongang.domain.BoardVO">
	select * from film_board where bno = #{bno}
</select>

<select id="getTotalCount" resultType="int">
	select count(bno) from film_board where bno >0
</select>


<select id="read" resultType="com.joongang.domain.BoardVO">
	select * from film_board where bno = #{bno}
</select>


<insert id="insert" useGeneratedKeys="true"  keyProperty="bno">
	insert into film_board (title, content, writer) values(#{title}, #{content}, #{writer})
</insert>

<update id="update">
	update film_board set title=#{title}, content=#{content} where bno = #{bno}
</update>

<delete id="delete">
	delete from film_board where bno = #{bno}
</delete>

</mapper>